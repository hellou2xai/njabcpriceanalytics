.env
__pycache__/
*.pyc
node_modules/
dist/
.vite/
parquet_output/
Data/
user_data/
*.db
*.duckdb
*.xlsx
.claude/
.DS_Store
Thumbs.db

# Generated enrichment artifacts (backfill logs + inspection exports)
goupc_*.log
goupc_test_*.zip

# Mobile screenshot scratch output (capture_mobile.mjs)
frontend/.mobile-shots/

# Local backup of R2 product images (large; created by backup_r2_images.py /
# transparent_images.py) and Playwright verification scratch.
image_backup/
frontend/.verify/

# Local working doc (incrementally drafted, not part of the repo)
howto.md

# Ad-hoc Playwright verification scripts and their screenshot output (one-off,
# per-session; not tests we run on CI).
frontend/verify_*.mjs
frontend/.verify-shots/

# Golden-snapshot scratch from manual API diffing sessions.
tests/golden/after*/

# One-off analysis scripts kept locally.
analyze_*.py
backend.log
